# ADR-002: Workspace and Brand Boundary

Decision: every business entity carries `workspace_id`; relevant entities also carry `brand_id` and nullable `client_id`. Active Owner membership is derived server-side. EG is not seeded or treated as the default主体.
