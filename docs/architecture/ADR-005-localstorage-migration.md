# ADR-005: LocalStorage Migration

Decision: migration is previewed and explicit. Legacy Mock never becomes Actual. Confidential Business Memory is not bulk-imported. Existing keys remain until Owner verifies database counts and rollback readiness.
