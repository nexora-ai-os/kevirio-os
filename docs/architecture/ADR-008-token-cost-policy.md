# ADR-008: Token and Cost Policy

Decision: deterministic code handles money, state and dedupe. Provider requests use selected fields, allowlisted purposes, input/output caps, server model policy, bounded retry, idempotency, monthly budget and cache.
