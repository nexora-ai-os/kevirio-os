# Incident Response Plan

Detect → contain feature/provider access → preserve sanitized audit evidence → identify workspace/data class → rotate affected credentials → assess notification duties → recover from verified backup → document corrective actions.

Never weaken RLS, delete evidence, retry external actions or expose raw customer content during investigation.
