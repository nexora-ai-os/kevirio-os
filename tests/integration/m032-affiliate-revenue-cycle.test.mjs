import test from "node:test";
import assert from "node:assert/strict";
import fs from "node:fs/promises";
const sql=await fs.readFile("supabase/migrations/032_affiliate_revenue_cycle.sql","utf8");
test("M032 is additive and creates no synthetic business object",()=>{for(const value of["affiliate_cycle_publications","affiliate_cycle_performance","affiliate_revenue_candidates","affiliate_revenue_evidence","affiliate_revenue_candidate_id","origin_type"])assert.ok(sql.includes(value),value);for(const denied of[/insert into public\.opportunities/i,/insert into public\.campaigns/i,/insert into public\.tasks/i,/insert into public\.affiliate_strategies/i])assert.doesNotMatch(sql,denied)});
test("M032 keeps Actual in revenue_records behind verified affiliate evidence",()=>{assert.match(sql,/insert into public\.revenue_records/);assert.match(sql,/verification_status='VERIFIED'/);assert.match(sql,/reward_state='CONFIRMED'/);assert.match(sql,/status='CANDIDATE'/);assert.match(sql,/unique references public\.affiliate_revenue_candidates/);assert.match(sql,/on conflict\(affiliate_revenue_candidate_id\) do nothing/)});
test("M032 is owner-private, optimistic, idempotent and externally locked",()=>{assert.equal((sql.match(/force row level security/g)||[]).length,1);for(const value of["resolve_personal_workspace()","p_expected_version","idempotency_key","external_execution","LOCKED","revoke all","grant execute"])assert.ok(sql.toLowerCase().includes(value.toLowerCase()),value);assert.doesNotMatch(sql,/grant (insert|update|delete) on public\.affiliate_/i)});
test("M032 emits sanitized timeline and audit metadata",()=>{assert.match(sql,/insert into public\.operational_activity_events/);assert.match(sql,/insert into public\.audit_logs/);assert.match(sql,/metadata_sanitized/);});
