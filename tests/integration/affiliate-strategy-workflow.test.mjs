import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";
const read=p=>readFile(new URL(`../../${p}`,import.meta.url),"utf8");
test("Research to Strategy uses exact M031 owner-protected path",async()=>{const[api,ui,repo]=await Promise.all([read("api/ai.js"),read("src/components/affiliate-v2/AffiliateProgramMaster.jsx"),read("src/repositories/affiliateStrategyRepository.js")]);for(const x of["affiliateStrategyPrepare","resolveVerifiedOwnerWorkspaceContext","prepare_affiliate_strategy","AFFILIATE_STRATEGY_CONTEXT_NOT_FOUND","paidAiJpy:0","externalExecution:\"LOCKED\""])assert.ok(api.includes(x),x);for(const x of["このResearchから戦略を作る","Strategy Draft · Owner Review","Owner確認してcanonical保存","Canonical Strategy","Program link:"])assert.ok(ui.includes(x),x);for(const x of["review_affiliate_strategy","confirm_affiliate_strategy","affiliate_strategies"])assert.ok(repo.includes(x),x);assert.doesNotMatch(api,/insert into public\.opportunities|APPLICATION/)});
