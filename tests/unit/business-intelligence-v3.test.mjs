import test from "node:test";import assert from "node:assert/strict";
import {buildBusinessIntelligenceV3,intelligenceHasNoFabricatedSuccess} from "../../src/domain/businessIntelligenceV3.js";
test("intelligence exposes required projections and preserves Unknown when empty",()=>{const x=buildBusinessIntelligenceV3();for(const key of ["organizationHealth","businessHealth","executiveDashboard","commandCenter","digitalTwin","aiWorkforce","businessMemory","knowledgeGraph","opportunityRadar","riskRadar","timeline","capabilityMap","operationalBlockers"])assert.ok(key in x);assert.equal(x.digitalTwin.truthClass,"Unknown");assert.equal(x.executiveDashboard.actual.actualRevenue,"Unknown");assert.equal(x.externalExecution,false)});
test("Actual and Forecast remain separate without fabricated zero",()=>{const x=buildBusinessIntelligenceV3({affiliate:{actualFinancials:{actualRevenue:100,actualCost:"Unknown",netProfit:"Unknown",roi:"Unknown",truthClass:"Unknown"}},revenue:{forecast:[{revenueMinor:500,truthClass:"Forecast"}]}});assert.equal(x.executiveDashboard.actual.actualRevenue,100);assert.equal(x.executiveDashboard.actual.actualCost,"Unknown");assert.equal(x.executiveDashboard.forecast[0].truthClass,"Forecast")});
test("partial source failure becomes an operational blocker",()=>{const x=buildBusinessIntelligenceV3({availability:{companyCore:"unavailable",revenue:"available",affiliate:"available",aiEmployees:"available"}});assert.equal(x.partial,true);assert.equal(x.operationalBlockers[0].truthClass,"Unknown")});
test("Actual timeline requires timestamp source and evidence",()=>{const bad=buildBusinessIntelligenceV3({affiliate:{timeline:[{truthClass:"Actual"}]}});assert.equal(intelligenceHasNoFabricatedSuccess(bad),false);const good=buildBusinessIntelligenceV3({affiliate:{timeline:[{truthClass:"Actual",timestamp:"2026-08-05T00:00:00Z",source:"ledger",evidence:"e"}]}});assert.equal(intelligenceHasNoFabricatedSuccess(good),true)});
