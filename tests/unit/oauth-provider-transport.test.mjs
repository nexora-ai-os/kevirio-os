import test from "node:test";import assert from "node:assert/strict";import {createOAuthCodeExchange} from "../../server/oauthProviderTransport.js";
test("missing oauth credential fails closed without transport",()=>assert.equal(createOAuthCodeExchange("google",{},()=>{throw new Error("called")}),null));
test("oauth exchange normalizes tokens without exposing client secret",async()=>{let call;const exchange=createOAuthCodeExchange("google",{GOOGLE_CLIENT_ID:"id",GOOGLE_CLIENT_SECRET:"secret"},async(url,options)=>{call={url,options};return {ok:true,json:async()=>({access_token:"access",refresh_token:"refresh",expires_in:60,scope:"read one"})}});const result=await exchange({code:"code",verifier:"verifier",redirectUri:"https://app.test/callback"});assert.equal(result.accessToken,"access");assert.deepEqual(result.scopes,["read","one"]);assert.equal(call.options.headers.authorization,undefined);assert.equal(String(call.url).includes("secret"),false)});
