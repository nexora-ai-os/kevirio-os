import test from"node:test";import assert from"node:assert/strict";import{AI_AREA_CAPABILITIES,AI_BUDGET_OPTIONS,AI_ROUTER_POLICY,buildAiRequestContract,routeAiRequest}from"../../src/services/aiRealOperations.js";
test("all 22 canonical areas have one bounded AI capability",()=>{assert.equal(Object.keys(AI_AREA_CAPABILITIES).length,22);for(const value of Object.values(AI_AREA_CAPABILITIES))assert.ok(value.length>5)});
test("router is free-only, private, truth-aware, and has no paid fallback",()=>{assert.equal(AI_ROUTER_POLICY.monthlyBudgetJpy,0);assert.equal(AI_ROUTER_POLICY.paidFallback,false);assert.equal(AI_ROUTER_POLICY.privacy,"PERSONAL_PRIVATE");assert.deepEqual(AI_BUDGET_OPTIONS,[0,500,1000,3000,"custom"])});
test("request contract requires identity workspace and rejects credential-like input",()=>{const base={authenticatedUserId:"u",workspaceId:"w",feature:"affiliate",text:"案件を比較"};assert.equal(buildAiRequestContract(base).costPolicy,"FREE_ONLY");assert.throws(()=>buildAiRequestContract({...base,text:"api_key=secret"}),/AI_PRIVATE_CONTEXT_BLOCKED/);assert.throws(()=>buildAiRequestContract({...base,workspaceId:null}),/AI_IDENTITY_CONTEXT_REQUIRED/)});
test("Owner-confirmed Gemini is primary free AI and never falls back paid",()=>{const contract=buildAiRequestContract({authenticatedUserId:"u",workspaceId:"w",feature:"assistant",text:"今日の優先順位"});assert.equal(routeAiRequest(contract).state,"CONNECTED_FREE");assert.deepEqual(routeAiRequest(contract,{geminiFreeEligibilityVerified:false}),{provider:"local-deterministic",model:null,state:"FREE_TIER_UNVERIFIED",reasonCode:"GEMINI_FREE_ELIGIBILITY_UNVERIFIED",costClassification:"FREE",paidFallback:false,externalAction:false})});
