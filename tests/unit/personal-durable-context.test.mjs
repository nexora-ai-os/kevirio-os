import assert from"node:assert/strict";import test from"node:test";import{buildTodayActions}from"../../src/repositories/personalContextRepository.js";
test("Today uses deterministic user-readable reasons",()=>{const actions=buildTodayActions({content:[{id:"c",title:"Draft",payload:{status:"DRAFT"}}],opportunities:[{id:"o",title:"Job",payload:{status:"APPLYING"}}],revenueState:"UNKNOWN"});assert.deepEqual(actions.map(x=>x.reason),["保存済みの下書きが未完了です","案件の状態が「応募準備中」です","Actual Revenueの状態を確認できていません。未確認を0円とは扱いません"])});
test("Today excludes completed and saved-only work",()=>assert.deepEqual(buildTodayActions({content:[{id:"c",payload:{status:"PUBLISHED"}}],opportunities:[{id:"o",payload:{status:"SAVED"}}],revenueState:"KNOWN"}),[]));
test("Today guides WON work to Revenue without inventing money",()=>{const actions=buildTodayActions({content:[],opportunities:[{id:"work-1",title:"受注仕事",payload:{status:"WON",active_work:{state:"ACTIVE"}}}],revenueCandidates:[],retrospectives:[],revenueState:"KNOWN"});assert.equal(actions[0].path,"/revenue");assert.match(actions[0].reason,/自動推測しません/)});
test("Today uses authorized retained learning state",()=>{const actions=buildTodayActions({content:[],opportunities:[],revenueCandidates:[{id:"candidate-1",title:"改善案件",payload:{}}],retrospectives:[],revenueState:"KNOWN"});assert.equal(actions[0].path,"/projects/retrospective");assert.match(actions[0].reason,/振り返り/)});
