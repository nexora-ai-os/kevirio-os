import assert from"node:assert/strict";import test from"node:test";import{validateResearchExecution}from"../../server/researchExecutionContract.js";
const valid={source:{canonicalUrl:"https://example.com/report",sourceName:"Example",sourceDomain:"example.com",sourceType:"OFFICIAL_COMPANY",reliabilityClass:"PRIMARY",costClass:"FREE_CONFIRMED"},findings:[{domain:"OPPORTUNITY",observedAt:"2026-08-22T00:00:00Z",statement:"A public market signal was observed.",truthClass:"WEB_SOURCE",confidence:.8,provenance:{source:"OWNER_EXPLICIT"}}]};
test("research contract accepts bounded free sourced findings",()=>assert.equal(validateResearchExecution(valid).ok,true));
test("research contract denies paid unknown unsafe and L3-shaped data",()=>{for(const costClass of["PAID","UNKNOWN"])assert.equal(validateResearchExecution({...valid,source:{...valid.source,costClass}}).ok,false);assert.equal(validateResearchExecution({...valid,findings:[{...valid.findings[0],statement:"password=secret"}]}).ok,false)});
