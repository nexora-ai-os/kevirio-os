import test from "node:test";import assert from "node:assert/strict";import{generateAffiliateStrategyDraft,normalizeAffiliateStrategy}from"../../server/affiliateStrategyGeneration.js";
test("strategy normalizer preserves unknown and truth boundary",()=>{const r=normalizeAffiliateStrategy('```json\n{"targetAudience":null,"recommendedChannels":["Content"]}\n```');assert.equal(r.ok,true);assert.equal(r.draft.targetAudience,null);assert.deepEqual(r.draft.recommendedChannels,["Content"]);assert.equal(r.draft.evidenceStatus,"NOT_EVIDENCE")});
test("strategy generation stays Gemini FREE and rejects invalid JSON",async()=>{const r=await generateAffiliateStrategyDraft({program:{id:"p"},finding:{id:"r"}},{credential:"fixture",dispatch:async()=>({ok:true,text:"not-json",finishReason:"STOP",rawLength:8})});assert.equal(r.ok,false);assert.equal(r.reasonCode,"AFFILIATE_STRATEGY_INVALID_RESPONSE");assert.equal(r.paidAiJpy,0);assert.equal(r.externalExecution,"LOCKED")});
