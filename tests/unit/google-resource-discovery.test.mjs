import test from "node:test";import assert from "node:assert/strict";import {createOAuthCipher} from "../../server/oauthRuntime.js";import {discoverGoogleResources} from "../../server/googleResourceDiscovery.js";
const scopes=["analytics.readonly","webmasters.readonly","youtube.readonly"].map(value=>`https://www.googleapis.com/auth/${value}`),key="test-only-key-that-is-at-least-thirty-two-bytes",cipher=createOAuthCipher(key),client={from:()=>({select(){return this},eq(){return this},maybeSingle:async()=>({data:{state:"connected",token_ciphertext:cipher.encrypt("token"),refresh_token_ciphertext:cipher.encrypt("refresh"),token_expires_at:new Date(Date.now()+3600000).toISOString(),granted_scopes:scopes},error:null})})};
test("Google resource discovery is bounded and read-only",async()=>{const calls=[],result=await discoverGoogleResources({client,workspaceId:"w",encryptionKey:key,transport:async(url,options)=>{calls.push({url,options});if(url.includes("analyticsadmin"))return{ok:true,json:async()=>({accountSummaries:[]})};if(url.includes("webmasters"))return{ok:true,json:async()=>({siteEntry:[]})};return{ok:true,json:async()=>({items:[]})}}});assert.equal(result.ok,true);assert.deepEqual(result.requestBounds,{analytics:20,searchConsole:20,youtube:5});assert.equal(result.writes,0);assert.equal(result.polling,false);assert.ok(calls.every(call=>call.options.method==="GET"));});
