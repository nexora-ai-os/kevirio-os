import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{buildTodayActions,loadAuthorizedPersonalContext}from"../../repositories/personalContextRepository.js";import OperationalCommandSurface from"./OperationalCommandSurface.jsx";
export default function NextPersonalHome({client}){const[reloadKey,setReloadKey]=useState(0),[state,setState]=useState({loading:true,context:null,error:null});useEffect(()=>{let active=true;setState({loading:true,context:null,error:null});loadAuthorizedPersonalContext(client).then(context=>{if(active)setState({loading:false,context,error:null})}).catch(error=>{if(active)setState({loading:false,context:null,error})});return()=>{active=false}},[client,reloadKey]);if(state.loading)return <main className="next-workspace" aria-busy="true"><p role="status">ホームを読み込んでいます。</p></main>;if(state.error)return <main className="next-workspace"><section className="next-card"><h1>ホームを表示できません</h1><p role="alert">通信状態またはログイン状態を確認してください。</p><button className="next-primary" onClick={()=>setReloadKey(value=>value+1)}>再読み込み</button></section></main>;const actions=buildTodayActions(state.context);const count=(items,status)=>status==="KNOWN"?items.length:"未確認";return <main className="next-workspace"><header className="next-header"><div><span>KEVIRIO NEXT</span><h1>ホーム</h1><p>今日の次の一歩を、自分がアクセスできる実データから確認します。</p></div></header><div className="next-grid next-grid--home"><section className="next-card next-card--wide"><header><h2>今日やること</h2></header>{actions.length?<ul className="next-list">{actions.map(action=><li key={action.id}><b>{action.title}</b><span>理由: {action.reason}</span><Link className="next-link next-inline-link" to={action.path}>開く</Link></li>)}</ul>:<div className="next-empty"><strong>続きが必要な作業はありません</strong><p>新しい成果物を作るなら、Threadsの下書きから始められます。</p><Link className="next-link next-inline-link" to="/content">下書きを作る</Link></div>}</section><section className="next-card"><header><h2>コンテンツ</h2></header><b>{count(state.context.content,state.context.contentState)}</b><p>自分の保存済み記録</p></section><section className="next-card"><header><h2>案件</h2></header><b>{count(state.context.opportunities,state.context.opportunityState)}</b><p>自分の登録済み案件</p></section><section className="next-card"><header><h2>収益</h2></header><b>{count(state.context.revenue,state.context.revenueState)}</b><p>見込みや未確認を確定売上として表示しません。</p></section></div><OperationalCommandSurface client={client} showCapture heading="全領域"/></main>}
